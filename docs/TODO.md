
### Core Features
1. Send out emails to multiple recipients.
2. Send out emails to multiple recipients with attachments.
3. Google Authentication or something like that.
4. (optional) have the email sending be in the same page as the pdf generation so no need to upload zip files anymore... but let's see.

### UI improvements


### Email
1. Use Elastic Mail (follow email-app implementation)
2. Use Email database from internal tools 
3. just disable the send email part for now

### Authentication & Security
1. Login (use OTP + whitelist email?)
2. Whitelist specific domains (e.g. @propertyone.com.ph)
3. Have OTP Confirmation right before sending out emails.
4. IP whitelist serendra and ensure no one else can access this tool.